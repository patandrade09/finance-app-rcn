import { StyleSheet } from "react-native";

export default StyleSheet.create({
  safeArea: {
    marginTop: 40,
  },
  mainContainer: {
    flexDirection: "column",
    alignItems: "center",
  },
  mainImage:{
    width: 200,
    height: 200,
  },
  titleContainer: {
    marginLeft:15
  },
  subTitleContainer: {
    marginBottom:10
  },
  inputContainer: {
    flexDirection: "column",
    alignItems: "center",
  }
});
